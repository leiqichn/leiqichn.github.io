/// <reference types="node" />
import { Config, Result } from './types';
export declare class TinyPNG {
    private instance;
    constructor(apiKey: string);
    compress(data: string | Buffer, config?: Config): Promise<Result>;
}
export * from './types';
